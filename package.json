{
  "name": "newspaper",
  "private": true,
  "workspaces": [
    "skill",
    "web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=web",
    "build": "npm run build --workspace=web",
    "test": "npm run test --workspace=skill",
    "fetch": "npm run fetch --workspace=skill"
  }
}
